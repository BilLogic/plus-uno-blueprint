-- Superseded by 20260726215000_goal_setting_named_path_type.sql.
-- Goal Setting title-based paths use path_type `named` (not happy/exception).
-- Kept as a no-op so migration history stays contiguous on already-applied envs.

select 1;
